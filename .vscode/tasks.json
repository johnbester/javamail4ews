{
    "version": "2.0.0",
    "configurations": [
        {
            "label": "compile",
            "type": "shell",
            "command": "mvn -B compile package",
            "group": "build"
        }
    ]
}